# Bundled Binaries

Place these executable files in this directory before building or publishing:

```text
yt-dlp.exe
ffmpeg.exe
ffprobe.exe
```

They are ignored by Git by default. Clip copies them to the build output when present.
