<UserControl
    x:Class="Clip.Views.SaveLocationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CompactGlassCardBorderStyle}">
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Header="Save to"
                     Text="{Binding SaveDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="False"
                     CornerRadius="12" />
            <Button Grid.Column="1"
                    VerticalAlignment="Bottom"
                    Style="{StaticResource IconButtonStyle}"
                    Command="{Binding ChooseFolderCommand}"
                    ToolTipService.ToolTip="Choose folder">
                <SymbolIcon Symbol="Folder" />
            </Button>
        </Grid>
    </Border>
</UserControl>
