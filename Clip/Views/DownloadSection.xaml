<UserControl
    x:Class="Clip.Views.DownloadSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Clip.Views">
    <Border Style="{StaticResource GlassCardBorderStyle}">
        <Grid RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Queue" FontWeight="SemiBold" FontSize="18" />
                    <TextBlock Text="{Binding QueueSummary}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>
                <ToggleButton Grid.Column="1"
                              IsChecked="{Binding IsPaused, Mode=TwoWay}"
                              CornerRadius="18"
                              Padding="12,7">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <SymbolIcon Symbol="Pause" />
                        <TextBlock Text="Pause" />
                    </StackPanel>
                </ToggleButton>
            </Grid>

            <TabView Grid.Row="1"
                     IsAddTabButtonVisible="False"
                     CanDragTabs="False"
                     TabWidthMode="Equal">
                <TabViewItem Header="Downloads">
                    <views:DownloadList />
                </TabViewItem>
                <TabViewItem Header="History">
                    <views:HistoryView />
                </TabViewItem>
            </TabView>
        </Grid>
    </Border>
</UserControl>
