<UserControl
    x:Class="Clip.Views.ContentView"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Clip.Views"
    AllowDrop="True"
    DragOver="OnDragOver"
    Drop="OnDrop">
    <Grid Padding="16,0,16,14" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <views:UpdateBanner />

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Spacing="10">
                    <views:URLInput />
                    <views:VideoPreview />
                    <views:FormatPicker />
                    <views:ClipRangeView />
                    <views:SaveLocationView />
                    <views:Settings DataContext="{Binding Settings}" />
                </StackPanel>
            </ScrollViewer>

            <views:DownloadSection Grid.Column="1" DataContext="{Binding Downloads}" />
        </Grid>

        <views:StatusBarView Grid.Row="2" />
    </Grid>
</UserControl>
