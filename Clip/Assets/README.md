# Assets

Add PNG logo assets here before producing an MSIX package:

```text
StoreLogo.png
Square44x44Logo.png
Square150x150Logo.png
```

The default project is configured for an unpackaged desktop build, so these are only required when enabling MSIX packaging.
